# JS Error Lab

A modern, production-ready React application for debugging and analyzing JavaScript errors. Built with **Vite**, **React**, **Tailwind CSS**, and **shadcn/ui**.

## ✨ Features

- ⚡ **Vite** - Lightning-fast build tool
- ⚛️ **React 18+** - Modern React with hooks
- 🎨 **Tailwind CSS** - Utility-first CSS framework
- 🧩 **shadcn/ui** - High-quality UI components
- 🌙 **Dark Mode** - Enabled by default
- 📁 **Clean Structure** - Well-organized folder layout
- 🔧 **TypeScript** - Type-safe development
- 📦 **Production-Ready** - Optimized and ready to deploy

## 📁 Project Structure

```
src/
├── components/        # Reusable React components
│   └── ui/           # shadcn/ui components
├── errors/           # Error-related components and handlers
├── utils/            # Utility functions
│   ├── errorHandler.ts
│   �│   �│   �│   �│   �│   �│   �│   �│   �│   �│   �│   �│   �│   �│   �│   ��point
└── index.css         # Global styles with Tailwind
```

## 🚀 Quick Start

### Installation

```bash
npm install
```

### Development

```bash
npm run dev
```

Open `http://localhostOpen `http://localhostOpen `httpoduction BuOpen `http://localhostOpen `http://localhork ModeOpen `http://localhostOpen `http://localhostOpen  dark mode enabled. Toggle between light and dark modes using the button in the header.

## 🧩 ## 🧩 ## 🧩 ## 🧩 ## 🧩 ## 🧩 ## 🧩 ## �
shnpx shadcn-ui@latest add [component-name]
```

#######################################################h:

- Dark mode support
- Custom color- Criables
- Production optimization

## 📚 Available Scripts

| Command | Description |
|---------|-------------|---------|-------------|---------|-------------|---------|------- B|---------|-------------|---------|-------| Prev|---------|-------------|---m run lint` | Run ESLint |

## 🚀 Deployment

Ready to deploy on:

- Vercel
- Netlify
- GitHub Pages
- AWS S3 + CloudFront

Run `npm run bRun `npm run bRun `npm run bRun `npm run bRun `npm run bIT License

---

**Happy coding!** 🎉
